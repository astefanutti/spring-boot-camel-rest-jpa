camel:
  springboot:
    name: CamelRestJpa
  component:
    servlet:
      mapping:
        enabled: true
        context-path: /camel-rest-jpa/*

logging:
  level:
    root: INFO

quickstart:
  generateOrderPeriod: 1s
  processOrderPeriod: 3s
